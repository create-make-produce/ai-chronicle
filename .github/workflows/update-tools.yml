# update-tools.yml - 廃止（V8以降不要）
# ツール名・タグライン・概要は登録時から変更不要のため停止
# ローンチ収集は collect-new-tools.yml に統合済み

name: ツール情報更新（廃止）

on:
  # スケジュール削除 → 手動実行のみ（念のため残存）
  workflow_dispatch:

jobs:
  update-tools:
    runs-on: ubuntu-latest
    steps:
      - name: 廃止済み
        run: echo "このワークフローは廃止されました。collect-new-tools.yml を使用してください。"
